# DO NOT COMMIT REAL SECRETS
# This is a template - use sealed-secrets, external-secrets, or Azure Key Vault CSI
apiVersion: v1
kind: Secret
metadata:
  name: scholarly-secrets
  namespace: scholarly
  labels:
    app.kubernetes.io/name: scholarly
type: Opaque
stringData:
  # These values should be injected via CI/CD or secret management
  DATABASE_URL: "postgresql://user:password@host:5432/scholarly?sslmode=require"
  JWT_SECRET: "your-jwt-secret-minimum-32-characters-long"
