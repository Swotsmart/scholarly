fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
# SageMath — install via the system package manager in the Dockerfile
# (sagemath-standard pip wheel is large; the Docker image handles this)
